import 'package:flutter/material.dart';
/**
 * 产品属性扩展
 * 使用路由 get.to(New Page) 到某个类，实现添加内容后返回
 */

/// 属性
class ProductAttr extends StatelessWidget {
  const ProductAttr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

/// 规格
class ProductSpec extends StatelessWidget {
  const ProductSpec({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

/// 描述
class ProductDesc extends StatelessWidget {
  const ProductDesc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
